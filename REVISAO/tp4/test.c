#include <stdio.h>
#include <stdlib.h>
#include "lista.c"
#include "lista.h"

int main(){
    struct lista_t *lst = lista_cria();

    //lista_insere(lst,1,0);
    //lista_insere(lst,2,1);
    //int rm = lista_retira(lst, &rm, 0);
    //lista_imprime(lst);


}
